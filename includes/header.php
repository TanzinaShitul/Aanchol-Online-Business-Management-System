<?php
// This partial is included inside each page's existing <body>.  Keeping it to
// navigation markup prevents nested <html>, <head>, and <body> elements,
// which can stop Bootstrap navigation controls from working consistently.
require_once __DIR__ . '/nav.php';
